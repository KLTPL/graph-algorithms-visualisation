import { GraphComponentDirectedWeightedProps } from "../GraphContainer";

export default function Directed({ visualisationData }: GraphComponentDirectedWeightedProps) {
  return (
    <div>
      directed component
    </div>
  );
}