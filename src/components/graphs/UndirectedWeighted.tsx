import { GraphComponentDirectedWeightedProps } from "../GraphContainer";

export default function UndirectedWeighted({ graphData, data }: GraphComponentDirectedWeightedProps) {
  return (
    <div>
      undirected weighted component
    </div>
  );
}