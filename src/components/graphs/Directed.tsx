import { GraphComponentDirectedWeightedProps } from "../GraphContainer";

export default function Directed({ graphData, data }: GraphComponentDirectedWeightedProps) {
  return (
    <div>
      directed component
    </div>
  );
}